import React from 'react'
import { IButtonTopBar } from '../type'
import { Button } from '@mui/material'



const TopBarButton = (props: IButtonTopBar) => {
    return (
        <Button />
    )
}

export default TopBarButton
